<!DOCTYPE html>
<html>
	<head>
		<% include ../headerStuff %>
	</head>
	<body>
		<% include ../pageHeader %>

		<div class="container">
			<h2>Test Page</h2>
			<hr role="separator" />

		</div>
		<div class="container">
			<form action="#">
				<div class="form-group">
					<label for="txtMail">eMail</label>
					<input id="txtMail" class="form-control" type="email" placeholder="eMail">
					
				</div>
				<div class="form-group">
					<label for="txtPassword">password</label>
					<input id="txtPassword" class="form-control" type="password" placeholder="Password">
				<div class="form-group">
					<label for="txtFile">File Selection</label>
					<input id="txtFile" type="file">
					<p class="form-text small">Here is the help for uploading.</p>
				</div>		
				<div class="checkbox">
					<label><input type="checkbox"> Save</label>
				</div>
					<button class="btn btn-secondary" type="submit">Save</button>
				</div>
			</form>
		</div>
		<% include ../pageFooter %>
		<% include ../footerStuff %>
	</body>
</html>
